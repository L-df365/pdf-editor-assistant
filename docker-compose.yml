version: '3.8'

services:
  pdf-editor:
    image: pdf-editor-assistant
    container_name: pdf-editor-assistant
    environment:
      - DISPLAY=${DISPLAY}
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix:rw
      - /home/user:/home/user
    network_mode: host
